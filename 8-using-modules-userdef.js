const sayHi = require('./5-utils')
const names = require('./4-names')
const addNum = require('./7-mind-granade') //loads and executes the whole file, see the console print.

sayHi(names.john)
sayHi(names.peter)
